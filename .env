# This file contains environment variables for the application.
# It is loaded by Docker Compose and used by the backend and worker services.

# --- PostgreSQL Database Connection ---
# These variables are used by the postgres service itself in docker-compose.yml
POSTGRES_USER=mediafetcher
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=mediafetcher_db

# This full URL is constructed from the variables above and used by the backend/worker apps.
# The hostname 'postgres' matches the service name in docker-compose.yml.
DATABASE_URL=postgresql+asyncpg://mediafetcher:secure_password@postgres/mediafetcher_db

# --- Redis Connection ---
# The hostname 'redis' matches the service name in docker-compose.yml.
REDIS_HOST=redis

# --- Backend API Settings ---
# Comma-separated list of origins allowed to make requests to the API.
# Use '*' for development, but a specific domain for production.
ALLOWED_ORIGINS=*

# --- Worker Settings ---
# The absolute path on the host machine where media files should be stored.
# This path is volume-mapped into the containers that need it.
NAS_MEDIA_PATH=/mnt/truenas/App-DL/media
